# Task T1: normalize coupon codes

Fix the coupon lookup bug in `capabilities/pricing/api.py`.

Coupon codes are case-insensitive and may have surrounding whitespace. For example, `discount_percent("  save10 ")` must return `10`. Unknown coupon codes must still return `0`, and the public function signatures in the pricing capability must not change.

`tests/test_t1_regression.py` is a pre-existing failing regression test in this worktree. Run the full unittest suite, make the smallest pricing-local fix, and keep that regression test in place. Do not skip, weaken, or delete tests.

