# Task T2: create a reserved order

Add this public function to `capabilities/orders/api.py`:

```python
def reserve_and_create_order(
    stock,
    order_id,
    sku,
    quantity,
    unit_price_cents,
    total_cents,
):
    ...
```

It must call the public `capabilities.inventory.api.reserve` boundary, create the order through the existing `create_order` function, and return `(order, updated_stock)`. It must not mutate the input stock mapping.

This feature crosses exactly one capability boundary: orders to inventory. Do not import pricing or notify from orders, do not reimplement inventory reservation, and do not change `shop.checkout` or `cli.py`.

Add focused coverage in `tests/test_t2_feature.py`, keep all existing tests green, and preserve the existing public functions.

