# Task T3: migrate checkout to capability APIs

Replace the monolithic implementation inside `shop.checkout` with composition through all four public capability modules:

- `capabilities.inventory.api.reserve`
- `capabilities.pricing.api.discount_percent` and `discounted_total`
- `capabilities.orders.api.create_order`
- `capabilities.notify.api.order_confirmation`

Preserve this public function signature exactly, including argument order:

```python
def checkout(
    order_id,
    sku,
    quantity,
    unit_price_cents,
    stock,
    coupon_code,
    recipient,
):
    ...
```

The returned mapping, validation behavior covered by the existing tests, and the compact sorted JSON line emitted by `python -m cli` must remain byte-for-byte compatible for existing inputs. `shop.py` must delegate reservation, pricing, order construction, and notification formatting instead of retaining those duplicated implementations.

Add migration-focused compatibility coverage in `tests/test_t3_migration.py`. Keep all existing tests green and do not change the CLI arguments or any existing capability public function signature.

