schema_version = 1

[scan]
include = ["**/*.py", "**/*.yaml", "**/*.json", "**/*.toml"]

[capabilities]
strategy = "children"
roots = ["."]
depth = 1

[tests]
patterns = ["**/test_*.py"]
association = "same-capability"

[documentation]
root_context_required = false
