#!/usr/bin/env sh

plugin_root=${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)}}
command -v uv >/dev/null 2>&1 || exit 127

exec uv run --no-project "$plugin_root/tools/garden_cli.py" "$@"
